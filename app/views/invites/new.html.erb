<h1>New invite</h1>

<h2>Create invite</h2>
<%= form_for @invite do |f| %>
  <%= f.select(:user_id, options_for_users_select(@users)) %>
  <%= f.hidden_field :event_id %>
  <%= f.hidden_field :rsvp %> <br>
  <%= f.submit %>
<% end %>

<%= link_to 'Back', invites_path %>
